/* var numbers = [8, 7, 6, 5, 4, 3];
for(var i = 0; i < 7; i++){
    var number = numbers[i];
    console.log(number);
} */

// if we push or pop any array element then upper code will not be able to show that. So,
var numbers = [8, 7, 6, 5, 4, 3, 2, 1, 0];
for(var i = 0; i < numbers.length; i++){
    var number = numbers[i];
    console.log(number);
}